# 像素美術能力 Benchmark —— qwen38 (base) 畫得出什麼、用什麼畫、要多大

> 目的:量出 base 模型畫 game sprite 的**能力邊界**,推導出 SNES 美術管線該怎麼接。
> 這是常設測試:改 `SUBJECTS / SIZES / METHODS` 重跑 `gen_sweep.py`、`gen_matrix.py` 即可。
> 建於 2026-08-26。

---

## 1. 背景 / 動機（為什麼做這個）

先前實驗確立兩件事:
- **模型不會吐「原始像素索引格」**(16×16 / 32×32 的數字陣列 → 退化成空白)。原始像素是「難層」。
- **模型用 code(SVG / canvas)畫像素很強**(web 版那些像素 sprite 就是 code 畫的)。code 是它 pre-training 就流暢生成的 **native** 表徵。

由此帶出兩個必須量化的問題,才能設計 SNES 美術管線:
1. **要多大級數才畫得出來?**——不同複雜度的資產(小圖示 vs 角色)辨識所需的最小解析度不同。這決定 SNES 該用 16×16 還是 32×32 OBJ、或多 sprite 拼。
2. **用哪種「畫法」最好?**——SVG / canvas-code / ASCII-格 / JSON-格 哪個最 native、辨識度最高。這決定管線前端怎麼叫模型。

拿到這兩張表後,量化(壓到 SNES 合法 16 色 / 尺寸)是**下游、確定性**的一步(且已知「模型自壓 > 無情 PY median-cut」)。

---

## 2. 受測對象與變因

**主題(4 個,涵蓋複雜度光譜)**——刻意選記憶中「金幣/箭頭/戰士/火球全塌」那組硬骨頭:
| 代號 | 描述 | 複雜度 |
|---|---|---|
| `gold_coin` | 金幣(星星、光澤) | 低(單物件、對稱) |
| `fireball` | 火球(火焰) | 低–中(形狀鬆散) |
| `sax_rabbit` | 墨鏡爵士兔+金薩克斯 | 中–高(角色+道具+多色) |
| `warrior` | 劍/盾/頭盔/盔甲勇士 | 高(角色+多部件) |

**級數(解析度掃描)**:8 · 16 · 24 · 32 · 40 · 48 · 64 · 96 · 128
**畫法(方法 matrix)**:`svg`(<rect>) · `canvas`(JS 跑 headless 讀像素) · `ascii`(hex 字元格) · `json`(索引陣列, baseline)

---

## 3. 採樣 / 生成方式（怎麼跑的，可重現）

- **模型**:`qwen38`(base,**無 LoRA**),temp 0.5,OpenAI 相容 endpoint。
- 每格**單發**(不修、不自迴歸)——量的是**裸能力**,不是修復後結果。
- **SVG/canvas**:模型吐 code → `cairosvg` / headless chromium 光柵化到該級數 → NEAREST 放大顯示。
- **ascii/json**:模型吐字元格/索引陣列 → 直接 render。
- 每張原始輸出(SVG/JS/txt)+ PNG 全存 `sweep_out/`、`matrix_out/`(可回查、防我裁判時腦補)。
- 呈現:`sweep_montage.png`(主題×級數)、`matrix_montage.png`(方法×級數)。
- ⚠️ 洋紅底 = 透明色鍵未還原的 viewer cosmetic,不算扣分。

---

## 4. 評分標準（裁判規則）

**只評一個軸:辨識度 / 合主題**(0–10)。**美術天分、精緻度、配色美感一律不看。**

| 分數 | 意義 |
|---|---|
| 0 | 空白 / 純色 / 全然認不出 |
| 1–2 | 有東西但認不出是什麼 |
| 3–4 | 隱約像(要被告知才勉強看出) |
| 5–6 | 認得出大類(「是個角色/是圓的東西」)但關鍵特徵缺 |
| 7–8 | **一眼認得出主題**+多數關鍵特徵在(兔耳+墨鏡+金管 / 星星金幣) |
| 9–10 | 不用提示就明確是「那一個」,關鍵特徵齊全 |

判準錨點(避免我裁判漂移):
- `sax_rabbit` 關鍵特徵 = 兔耳 · 墨鏡 · 金色薩克斯 · 白身(4 項齊 = 9–10)
- `gold_coin` = 圓/金 · 星或紋 · 光澤(認得出圓金幣即 7+)
- `fireball` = 火焰形 · 橘紅漸層 · 向上尖(像火即 7+)
- `warrior` = 人形 · 武器(劍) · 盾/盔甲(人+劍即 6+,齊全 9+)

**同分不比美醜**;若兩格都「一眼認得出」都給 7–8,再依特徵齊全度微調。

---

## 5. 要看什麼 + 為什麼（這表拿來幹嘛）

- **表 A(解析度)找「最小可辨識級數」**:哪一欄開始跨過 7 分。
  - → 小圖示(金幣/火球)最小要多大? → 大概率 SNES **16×16 一格**就夠 → 最省 VRAM。
  - → 角色(勇士/兔兔)要多大才 7+? → 若要 ≥32 才認得出 → SNES 得用 **32×32 OBJ** 或多 sprite 拼,別硬塞 16×16。
  - 這直接變成「**資產類別 → SNES sprite 尺寸預算**」。
- **表 B(方法)找「最 native 畫法」**:哪個方法在各級數平均分最高。
  - → 決定管線前端叫模型用哪種格式(預期 svg/canvas 高、json 低)。
  - → 若 canvas > svg,值得把美術前端從 SVG 換成 canvas-code。

---

## 6. 表 A —— 解析度掃描（辨識度 0–10）

主題 × 級數(單發,base)：

| 主題＼級數 | 8 | 16 | 24 | 32 | 40 | 48 | 64 | 96 | 128 |
|---|---|---|---|---|---|---|---|---|---|
| gold_coin  | _ | _ | _ | _ | _ | _ | _ | _ | _ |
| fireball   | _ | _ | _ | _ | _ | _ | _ | _ | _ |
| sax_rabbit | _ | _ | _ | _ | _ | _ | _ | _ | _ |
| warrior    | _ | _ | _ | _ | _ | _ | _ | _ | _ |

**最小可辨識級數(跨 7 分的第一欄)**:_（待填）_

## 7. 表 B —— 方法 matrix（辨識度 0–10）

薩克斯兔兔 × 方法 × 級數：

| 方法＼級數 | 16 | 32 | 64 | 平均 | 備註 |
|---|---|---|---|---|---|
| **svg**    | 7 | 2 | **9** | 6.0 | 16 兔耳+墨鏡好;**32 是單發壞 roll(白條)**;64 四特徵全齊 |
| **canvas** | 4 | **7** | 0 | 3.7 | 32 整隻不錯;**64 空白(code 沒畫出)**;高天花板但高變異 |
| ascii  | 0 | 0 | 0 | 0.0 | 全空白(hex 字元格 = 非 native) |
| json   | 0 | 0 | 0 | 0.0 | 全塌(索引陣列 = 死穴,16 直接 FAIL) |

**最 native 畫法 = `svg`(最穩)**。canvas 天花板高(32 那格最完整)但**高變異、會整片空白**(需自迴歸才穩);ascii/json **確認是非 native,任何級數都塌**。
→ 前端定案:**SVG 為主**;canvas 當「要更細時」的選項但**必須配 playwright 驗證+重試**(單發不可靠)。
→ 註:svg 單發也會偶爾壞(32 那格)⇒ **實務要 2–3 次取最佳或加驗證**,但它壞的機率遠低於 canvas 空白。

## 8. 結論與管線建議

**畫法**:SVG(native、最穩,方法 matrix 已定)。
**尺寸/縮放**:畫 **4× 目標**(16→64、32→128)再縮 4:1(整數倍 pixel-perfect)。64/128px 落在模型舒服區,4:1 縮不殘格。
**量化**:縮完保留 alpha 判透明(**別合成到洋紅底**,否則 LANCZOS 半透明邊 → 粉色雜邊),opaque 區 median-cut ≤15 色 + index0 透明。

**★STYLE 對照(`compare_style.py`,0–10 辨識度,`style_montage.png`):**

| 資產＼style | flat 純色塊 | shade 單邊陰影 | outline 有框 |
|---|---|---|---|
| 金幣 16 | 4(最好·星紋不保) | 2(純黃塊) | 2(過淡) |
| 火球 16 | 5 | **7 最佳**(火焰明確) | 2(不像火) |
| 兔兔 32 | **8 最佳**(墨鏡+粉鼻+金SAX全齊) | 7 | chibi+out **0 崩** |
| 兔兔 chibi 32 | chibi+flat 6(墨鏡讚·SAX糊) | — | — |

**★定案:預設 `flat`(純色塊),不強制。**
- `flat` = 全能王(角色拿全場最高 8·icon 也不崩)⇒ 全域預設。
- `shade` = 只在**有機/漸層物**(火焰/爆炸/煙)贏,硬邊 icon 會糊。
- `outline` = 全場最爛(prompt 逼畫框→淡而亂),降級。
- **chibi 不預設開**:32px 大頭會**擠掉招牌道具**(兔兔的薩克斯糊掉)。Q版只適合「靠臉認」的角色,「靠道具認」的別 chibi。
- **金幣類細紋 icon** 在 16 天生吃虧(星星不保)⇒ 要嘛簡化描述(純金盤+反光)、要嘛上 32。

**★工具**:`make_sprite.py "<subject>" --size 16 --style flat [--chibi]`(best-of-N + 不塌探針,永不出空白);批次對照 `compare_style.py`。管線=**SVG flat 畫 4× → 縮 4:1 保 alpha → ≤15 色 → CHR bitplane**(下游確定性)。
